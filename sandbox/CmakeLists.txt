file(GLOB_RECURSE SOURCES
    ./*.cpp
)

add_executable(sandbox ${SOURCES})

target_link_libraries(sandbox SDL2-static)

target_link_libraries(sandbox sycamore)

target_include_directories(sandbox PUBLIC ../)
