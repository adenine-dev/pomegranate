file(GLOB_RECURSE SOURCES
    ./*.cpp
)

add_executable(sandbox ${SOURCES})

set_target_properties(sandbox PROPERTIES WIN32_EXECUTABLE TRUE)
set_target_properties(sandbox PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${POM_OUTPUT_DIRECTORY})

target_link_libraries(sandbox pomegranate)

target_include_directories(sandbox PUBLIC ../)

assign_source_group(${SOURCES})
define_filename_macro(${SOURCES})
